apiVersion: v1
kind: Service
metadata:
  name: alfresco-svc-alfresco
  labels:
    app: alfresco-repo
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: alfresco-repo
