apiVersion: v1
kind: Service
metadata:
  name: alfresco-svc-nginx
  labels:
    app: alfresco-nginx
spec:
  type: LoadBalancer
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: alfresco-nginx
